# ReadTheDocs Sphinx build dependencies.
#
# Runtime deps come from the package itself (single source of truth =
# pyproject.toml): installing the `gui` extra pulls numpy/scipy/pandas/h5py +
# pyqt5/silx, which is what autodoc needs to import the pioner modules. The
# hardware (uldaq) and server (pytango) deps are NOT installed here -- they are
# mocked in docs/source/conf.py (autodoc_mock_imports).
.[gui]

# Sphinx tooling (doc-build only; not a project runtime dependency).
sphinx==6.2.1
myst-parser==2.0.0
sphinx-rtd-theme==1.3.0
